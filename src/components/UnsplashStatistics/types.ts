export type UnsplashStatisticsType = {
  downloads: {
    total: number;
    historical: {
      change: number;
    };
  };
  views: {
    total: number;
    historical: {
      change: number;
    };
  };
};
